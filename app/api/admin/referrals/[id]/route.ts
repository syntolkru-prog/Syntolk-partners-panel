import { NextRequest, NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { requireAdminAccess } from "@/lib/api-auth";
export async function GET(request:NextRequest,{params}:{params:Promise<{id:string}>}){const a=await requireAdminAccess(request);if(a.error)return a.error;const {id}=await params;const referral=await prisma.referral.findUnique({where:{id},include:{partner:{include:{group:true,program:true}},payments:{include:{commissions:true,refunds:true},orderBy:{paidAt:"desc"}}}});if(!referral)return NextResponse.json({error:"Not found"},{status:404});return NextResponse.json({referral});}
export async function PATCH(request:NextRequest,{params}:{params:Promise<{id:string}>}){const a=await requireAdminAccess(request);if(a.error)return a.error;const {id}=await params;const b=await request.json() as any;const data:any={};if(b.status!==undefined)data.status=b.status;if(b.metadata!==undefined)data.metadata=b.metadata;if(b.canceledAt!==undefined)data.canceledAt=b.canceledAt?new Date(b.canceledAt):null;return NextResponse.json({referral:await prisma.referral.update({where:{id},data})});}
