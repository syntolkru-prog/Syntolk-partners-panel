import { randomBytes } from "node:crypto";
import { NextRequest, NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { requireAdminAccess } from "@/lib/api-auth";
export async function GET(request:NextRequest){const a=await requireAdminAccess(request);if(a.error)return a.error;const hooks=await prisma.outgoingWebhook.findMany({include:{logs:{orderBy:{createdAt:"desc"},take:10}},orderBy:{createdAt:"desc"}});return NextResponse.json({webhooks:hooks});}
export async function POST(request:NextRequest){const a=await requireAdminAccess(request);if(a.error)return a.error;const b=await request.json() as any;if(!b.name||!b.url)return NextResponse.json({error:"name and url required"},{status:400});const hook=await prisma.outgoingWebhook.create({data:{name:b.name,url:b.url,secret:b.secret||randomBytes(24).toString("hex"),events:b.events||[],isActive:b.isActive===undefined?true:Boolean(b.isActive)}});return NextResponse.json({webhook:hook},{status:201});}
export async function PATCH(request:NextRequest){const a=await requireAdminAccess(request);if(a.error)return a.error;const b=await request.json() as any;if(!b.id)return NextResponse.json({error:"id required"},{status:400});const data:any={};for(const k of ["name","url","events","isActive"]){if(b[k]!==undefined)data[k]=b[k]}if(b.rotateSecret)data.secret=randomBytes(24).toString("hex");const hook=await prisma.outgoingWebhook.update({where:{id:b.id},data});return NextResponse.json({webhook:hook});}
export async function DELETE(request:NextRequest){const a=await requireAdminAccess(request);if(a.error)return a.error;const id=new URL(request.url).searchParams.get("id");if(!id)return NextResponse.json({error:"id required"},{status:400});await prisma.outgoingWebhook.delete({where:{id}});return NextResponse.json({success:true});}
